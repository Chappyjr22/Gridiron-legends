// Formation Lab edit-mode state, read by the simulation loop (to skip normal
// gameplay updates), the pointer handlers (to drag entities), and the UI
// (to toggle edit mode on/off).
export const editState={editMode:false,dragEntity:null};
