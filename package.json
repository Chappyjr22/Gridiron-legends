{
  "name": "gridiron-legends-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "npm@10.9.8",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "npm install --no-audit --no-fund && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "vite": "^7.1.3",
    "wrangler": "^4.128.0"
  }
}
